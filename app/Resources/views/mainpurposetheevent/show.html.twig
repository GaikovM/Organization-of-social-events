{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mainpurposetheevent</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mainPurposeTheEvent.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mainPurposeTheEvent.name }}</td>
            </tr>
            <tr>
                <th>Free</th>
                <td>{% if mainPurposeTheEvent.free %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('mainpurposetheevent_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('mainpurposetheevent_edit', { 'id': mainPurposeTheEvent.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
