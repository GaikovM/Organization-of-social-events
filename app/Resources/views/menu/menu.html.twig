<nav class="navbar navbar-default navbar-fixed-top">

    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">Мир мероприятий</a>
        </div>

        <div id="navbar" class="navbar-collapse collapse in">
            <form class="navbar-form navbar-left">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Поиск">
                </div>
                <button type="submit" class="btn btn-default">Поиск</button>
            </form>

            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a href="{{ path('feedback') }}">
                        О нас
                    </a>
                </li>
                <li>
                    <a href="{{ path('feedback') }}">
                        Обратная связь
                    </a>
                </li>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li><a href="{{ path('fos_user_profile_show') }}">
                            Пользователь {{ app.user.username }}
                        </a>
                    </li>

                    <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">Управление<span
                                    class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a tabindex="-1" href="{{ path('user_panel_index') }}">Управление мероприятиями</a></li>
                            <li class="dropdown-submenu">
                                <a class="test" tabindex="-1" href="#">Профиль <span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    <li><a tabindex="-1" href="{{ path('fos_user_profile_show') }}">Просмотр</a></li>
                                    <li><a tabindex="-1" href="{{ path('fos_user_profile_edit') }}">Редактирование</a>
                                    </li>
                                    <li><a tabindex="-1" href="{{ path('fos_user_change_password') }}">Сменить
                                            пароль</a></li>
                                </ul>
                            </li>
                        </ul>
                    </li>
                    <li><a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('fos_user_security_login') }}"
                           class="btn btn-default">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>